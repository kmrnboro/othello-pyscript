[project]
name = "othello-pyscript"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pytest>=9.0.2",
]

[dependency-groups]
dev = [
    "pytest-cov>=7.0.0",
]
